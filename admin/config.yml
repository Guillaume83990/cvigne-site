backend:
  name: git-gateway
  branch: main

locale: fr

media_folder: images/uploads
public_folder: /images/uploads

collections:
  - name: histoires
    label: Histoires
    label_singular: Histoire
    folder: content/histoires
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    summary: "{{titre}} — {{date}}"
    fields:
      - label: Titre
        name: titre
        widget: string
        hint: "Le titre de votre histoire"
      - label: Date
        name: date
        widget: date
        format: "YYYY-MM-DD"
        default: ""
      - label: Publié
        name: publie
        widget: boolean
        default: true
        hint: "Décochez pour sauvegarder sans publier"
      - label: Contenu
        name: body
        widget: markdown
        hint: "Écrivez votre histoire ici"